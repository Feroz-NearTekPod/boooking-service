// This file is no longer used as the authentication check is done directly in the bookingService
// Keeping as an empty file for now to avoid breaking imports

export const setupAuthInterceptors = () => {
  // Authentication is now handled in the service layer
  console.log('Authentication is now handled in the booking service');
};
